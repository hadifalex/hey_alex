

# real wake word using PicoVoice's Porcupine

This way it will be less taxing than always calling the `listen()` and `SST` pipelines.
Probably easier for the Pi's CPU.

Currently PicoVoice only allows access keys for those with a business email address. 
It will have to wait :)